  <form method="get" action="/quizes/">
  Tema:  <select name="search_tema">
            <option value="" selected></option>
            <option value="otro">Otro</option>
            <option value="humanidades">Humanidades</option>
            <option value="ocio">Ocio</option>
            <option value="ciencia">Ciencia</option>
            <option value="tecnologia">Tecnología</option>
          </select>
	Preguta: <input type="text" name="search" value="<% search %>" />
	<input type="submit" value="Buscar" />
  </form>
<table>
<tr>
   <th>Pregunta</th><th>Tema</th><th colspan="2">&nbsp;</th>
</tr>
<% var i; for (i=0; i < quizes.length; i++) { %>        
    <tr>
      <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td>
      <td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].tema %></a></td>
      <%if(session.user){%>
        <td><a href="quizes/<%= quizes[i].id %>/edit"><button>editar</button></a></td>
        <td>
          <form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
            <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
              borrar
            </button>
          </form>
        </td>
      <%}%>      
    </tr> 
<% } %>
</table><p>
<%if(session.user){%>
  <a href="/quizes/new"><button>Crear pregunta</button></a>
<%}%>